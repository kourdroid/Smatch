## 2024-03-14 - Convert Client-Side Pagination to SSR Links
**Learning:** The default Shadcn UI `PaginationLink` component implementation uses a native HTML `<button>` element with `onClick` event handlers (relying on `useRouter().push()` in `src/components/Pagination/index.tsx`). Search engine crawlers (like Googlebot) do not execute JavaScript routing logic, meaning they cannot discover or crawl pagination links built this way, severely harming the indexability of deeper content pages.
**Action:** When implementing pagination or any critical navigation components in Next.js, always use semantic `<a>` tags via the `next/link` component. When disabled states are needed for links, do not use the HTML `disabled` attribute; instead, rely on `aria-disabled="true"`, `tabIndex={-1}`, visual utility classes (`pointer-events-none opacity-50`), and `href="#"` to ensure accessibility and prevent Next.js from prefetching invalid routes.
## 2025-04-07 - Article JSON-LD for Actualites
**Learning:** The actualites module detail pages had FAQSchema but lacked the primary Article schema. The  utility in this codebase actually returns an `@type: 'Article'` schema and is meant to be used for posts and actualites as well as projects.
**Action:** Use `getProjectJsonLd` to inject Article structured data for news/articles content, and handle Payload CMS's potentially unpopulated media relationships (strings vs objects) safely when extracting image URLs for the schema.
## 2025-04-07 - Article JSON-LD for Actualites
**Learning:** The actualites module detail pages had FAQSchema but lacked the primary Article schema. The `getProjectJsonLd` utility in this codebase actually returns an `@type: 'Article'` schema and is meant to be used for posts and actualites as well as projects. PayloadCMS media relationships can be either an ID string or a populated object, which requires careful type guarding when generating schema.
**Action:** Use `getProjectJsonLd` to inject Article structured data for news/articles content, and handle Payload CMS's potentially unpopulated media relationships safely (e.g., `typeof image === 'object' && image !== null && 'url' in image`) when extracting image URLs for the schema.
