## 2024-05-23 - Chatbot Accessibility
**Learning:** Chatbot widgets often lack basic accessibility features like ARIA labels on inputs and live regions for dynamic message updates.
**Action:** Always verify `aria-label` on inputs and add `role="log"` with `aria-live` to message containers.
